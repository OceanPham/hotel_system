# Copy vào Render → Environment (Web Service → Environment)

SPRING_PROFILES_ACTIVE=prod

# Database (format giống project Node/Sequelize)
DB_HOST=180.93.43.186
DB_PORT=3306
DB_NAME=htms
DB_USERNAME=oceandb
DB_PASSWORD=your_password
# DB_DIALECT=mysql  ← không cần, project này luôn dùng MySQL

# JWT (bắt buộc)
JWT_SECRET=your_base64_secret_here

# Gmail SMTP (tùy chọn)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_FROM=your@gmail.com

# PORT do Render tự gán — không cần set
